{
  "name": "webpack-demo",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clear": "rm -rf dist",
    "dev": "yarn run clear && yarn run parcel src/*.html --no-cache",
    "build": "yarn run clear && yarn run parcel build src/*.html --out-dir ./",
    "now-build": "yarn build"
  },
  "dependencies": {
    "assemble": "~0.3.11",
    "autoprefixer": "^9.7.5",
    "cli": "^1.0.1",
    "grunt": "~0.4.0",
    "normalize.css": "^8.0.1",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-handlebars": "^0.4.4",
    "parcel-plugin-imagemin": "^4.0.1",
    "postcss-modules": "^1.4.1"
  },
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/preset-react": "^7.0.0",
    "sass": "^1.19.0",
    "webpack-cli": "^3.3.1"
  }
}
